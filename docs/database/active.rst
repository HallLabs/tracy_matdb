Active Database Documentation
=============================

The active database is primarily used by `mtp` and is not added to by
the user in a manual manner. The only thing the user should add to the
yml file regarding this group should be any changes that the user
wants to make to the calculator beg used here.

.. automodule:: matdb.database.active
   :synopsis: Database sub-classes for implementing the active databases.
   :members:      

